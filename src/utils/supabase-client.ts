/** @format */

// Supabase client disabled — commented out to remove Supabase usage.
// import { createClient } from '@supabase/supabase-js';
// import { projectId, publicAnonKey } from '/utils/supabase/info';
//
// const supabaseUrl = `https://${projectId}.supabase.co`;
//
// export const supabase = createClient(supabaseUrl, publicAnonKey);

// Placeholder export to avoid import errors in files that still import `supabase`.
// The Supabase integration has been intentionally disabled and API calls commented out.
export const supabase: any = null;
